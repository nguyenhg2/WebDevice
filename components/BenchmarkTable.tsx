import { gpus } from "@/lib/data";
import type { Benchmark } from "@/types";
export default function BenchmarkTable({rows,highlightGpu}:{rows:Benchmark[];highlightGpu?:string}){ return <div className="overflow-x-auto"><table className="w-full min-w-[720px] border-collapse text-sm"><thead><tr className="border-b"><th className="p-2 text-left">GPU</th><th>độ phân giải</th><th>Low</th><th>Medium</th><th>High</th><th>Ultra</th><th>Setting</th><th>Video</th></tr></thead><tbody>{rows.map((r,i)=>{const gpu=gpus.find(g=>g.slug===r.gpuSlug); return <tr key={i} className={"border-b "+(highlightGpu===r.gpuSlug?"bg-blue-50 dark:bg-blue-950":"")}><td className="p-2 font-semibold">{gpu?.name??r.gpuSlug}</td><td>{r.resolution}</td><td>{r.fpsLow}</td><td>{r.fpsMedium}</td><td>{r.fpsHigh}</td><td>{r.fpsUltra}</td><td>{r.recommendedSetting}</td><td><a className="text-blue-600" href={r.videoTestUrl??"#"} target="_blank" rel="nofollow">Xem</a></td></tr>})}</tbody></table></div> }
