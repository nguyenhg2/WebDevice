export default async function AdminLoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const params = await searchParams;

  return <section className="container max-w-md py-10"><h1 className="text-3xl font-black">Đăng nhập admin</h1><p className="mt-3 text-sm text-slate-600 dark:text-gray-300">Tài khoản mặc định: admin@maynaychoiduoc.vn / admin123. Hãy đổi `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET` trên production.</p>{params.error ? <p className="mt-4 rounded-md border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">Sai email hoặc mật khẩu.</p> : null}<form action="/api/admin/login" method="post" className="card mt-5 grid gap-3 p-4"><label className="grid gap-1 text-sm font-semibold">Email<input className="input" name="email" type="email" defaultValue="admin@maynaychoiduoc.vn" required /></label><label className="grid gap-1 text-sm font-semibold">Mật khẩu<input className="input" name="password" type="password" required /></label><button className="btn">Đăng nhập</button></form></section>;
}
